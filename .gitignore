# --- 依赖 (Dependencies) ---
node_modules/
.pnp
.pnp.js

# --- 构建产物 (Production) ---
dist/
dist-ssr/
*.tsbuildinfo

# --- 环境变量 (Environment Variables) ---
# ⚠️ 极其重要：永远不要提交带有密钥的 .env 文件
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# --- 日志 (Logs) ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# --- 测试覆盖率 (Testing) ---
coverage/

# --- IDE 和 编辑器配置 (Editor/IDE) ---
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# VS Code 配置 (通常建议忽略，除非团队有统一规范)
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
# 如果你不想提交任何 vscode 配置，直接写 .vscode/ 即可，把上面带 ! 的行删掉

# --- 操作系统文件 (OS) ---
Thumbs.db
ehthumbs.db
Desktop.ini